# EMBORGANIZER v6.0 Summary

Implemented:

- Super dashboard with GUI launch cards.
- Folder import option using multi-file/browser upload instead of a folder path text box.
- Removed visible local folder scan, image import legacy, image ZIP legacy, and scan image folder legacy options.
- Removed the visible high-res/4K image reader page.
- Added converter/import timer records showing how many designs converted and elapsed seconds.
- Replaced noisy converter raw JSON with clean detail chips/cards.
- Added `imgs_engine_v6.py` as a separated IMGS engine bridge for future engine upgrades.
